import React from 'react';
import List from '../Character copy/List';

function Home(props) {
    return (
        <div className= "container">
            <List/>
        </div>
    );
}

export default Home;